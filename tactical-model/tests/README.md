# TacticalModel tests

Run from the repository root:

```sh
godot --headless --path tactical-model --script res://tests/test_runner.gd
```

These tests are lightweight GDScript smoke/unit tests for formula tuning. They check config parsing, weapon/target loading, and key hit-chance relationships.
